<form method="post" action="{{ path('app_rapport_delete', {'id': rapport.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rapport.id) }}">
    <button class="btn">Delete</button>
</form>
